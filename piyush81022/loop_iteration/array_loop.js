const arr=['hello alma better',456,'javascript'];
for(let x in arr){
    console.log([x])
}
const student = {
    name: 'Monica',
    class: 7,
    age: 12
   }
   // using for...in
   for ( let key in student ) {
    // display the properties
   console.log(`${key} : ${student[key]}`);
   console.log(student[key])
   
   }
   console.log(student.toString)
   